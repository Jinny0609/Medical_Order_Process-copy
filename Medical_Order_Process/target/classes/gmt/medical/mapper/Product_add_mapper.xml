<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="gmt.medical.mapper.Product_add_mapper">
	<!-- product_info 테이블에 삽입 -->
	<insert id="insertProductInfo" parameterType="gmt.medical.model.ProductInfo" useGeneratedKeys="true" keyProperty="productId">
    INSERT INTO product_info (product_image, product_name, product_price, product_count, category_id)
    VALUES (#{productImage}, #{productName}, #{productPrice}, #{productCount}, #{categoryId})
	</insert>
	
	<!-- product_option 테이블에 삽입 -->
	<insert id="insertProductOption" parameterType="gmt.medical.model.ProductOption">
	    INSERT INTO product_option (product_id, option_name, option_price)
	    VALUES (#{productId}, #{optionName}, #{optionPrice})
	</insert>
	
	<!-- product_images 테이블에 삽입 -->
	<insert id="insertProductImages" parameterType="gmt.medical.model.ProductImages">
	    INSERT INTO product_images (product_id, image_path)
	    VALUES (#{productId}, #{imagePath})
	</insert>
</mapper>